apiVersion: 1

datasources:
  - name: ElasticSearch
    type: elasticsearch
    url: http://elasticsearch:9200
    access: proxy
    database: nio
    jsonData:
      timeField: "@timestamp"
      timeInterval: "5m"
